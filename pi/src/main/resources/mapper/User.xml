<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="team.pi.demo.mapper.UserMapper">

    <select id="findAll" resultType="User">
        select * from user;
    </select>

    <select id="selectUserByname" resultType="User">
        select * from user where username=#{username};
    </select>

    <insert id="insertUser" parameterType="User">
        insert into user(nickname,username,password,email,active,code) values(#{user.nickName},#{user.userName},#{user.password},#{user.email},#{user.active},#{user.code});
    </insert>

    <select id="selectUserByCode" resultType="User">
        select * from user where code=#{code};
    </select>

    <update id="updateStatusByUserName" parameterType="String">
        update user set active = 1 where username = #{username};
    </update>
</mapper>